public class _1hashing {
    
    public static void main(String[] args) {
        
        // hahsing is technique
        // use to make data structures like hashet hashmap

        //hashing technique makes , add remove nd search operation in O(1)
        //eg. hashset ,hashmap  - > but takes O(n) space 
        // just decrease time complexity

        // see , in array normally
        // search is O(n)
        // insert O(n)
        // delete O(n)
        // if sorted array O(log n) , by binary search
        // but O(1) is least  


        // data structure like ,BST(AVL tree... and its variaton)
        // makes all this operation in O(log n)

        //----------------------------------------------------------------------------------
        
        
    }
}
