
import java.util.Scanner;

public class _9scpace_complexity {
    public static void main(String[] args) {
        //  relation betwwen input size and memeory

        Scanner sc= new Scanner(System.in);
        int n = sc.nextInt();
        for (int j = 0; j < n; j++) {
            
        }
        // memory having i,j, only and if we want to give more inpute n , then no space will affect
        //  space complexity is constant


        // in case of array
        // if we want to make big aaray of input size (10^5)
        // then need more memory

        //  ,memory efefct computer 
        // memory affects read write operations from hardware
        // which increase time so we need to reduce space also



        sc.close();

        };
    }


    // we can also use space complexity interm of stack 
    //  O(no of funtion called (if recursion))