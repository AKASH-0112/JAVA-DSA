
public class _3time_space_recursive {
    public static void main(String[] args) {
        
        //  for time
        // O(n) - fact
        // O(logn) - binary search
        // O(nlogn) -- merge sort
        // O(2^n) -fibo
        

        //  space

        //  in recursive funtion , var takes k space totally
        // on each call , repeted space k
        // O( k * n) ===> O(n)== stack height

        //  binary search SC = O(k*logn) = O(log n)

        // merge sort sc =  k*log n + n(for array) == > O(n)  as log n is too smaller then  n 

        // fibo ,O(n)
    }
}
