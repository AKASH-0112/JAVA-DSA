
import java.util.ArrayList;

public class _2space {
    public static void main(String[] args) {
        


        //memory taken by programe
        // as function of length

        //  all var takes O(1)
        // even fixed size array takes O(1)
        // if array is of n size then O(n)


        //  below space comlexity is O(n) only
        // for loop memeory nhi leta ,vo bs time khata h
        for (int i = 0; i < args.length; i++) {
            
            ArrayList<Integer> a =new ArrayList<>();

            for (int j = 0; j < args.length; j++) {
                
            }
        }
    }
}
