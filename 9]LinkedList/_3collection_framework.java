import java.util.LinkedList;

public class _3collection_framework {
    public static void main(String[] args) {
        

        LinkedList<Integer> list =new LinkedList<>();
        //  -----------------
         list.addFirst(8);
         list.addLast(4);
         list.add(5);
         list.size();
         list.get(1);
         list.removeFirst();
         list.removeLast();
        //  list.remove("index");
        //  list.remove("object ");
    }
}
